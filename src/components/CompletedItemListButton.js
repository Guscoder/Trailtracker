import React from 'react';

const CompletedItemListButton = (props) => {
  return (
    <button type='button' className='btn btn-success btn-block'>
      Completed Items
    </button>
  );
};

export default CompletedItemListButton;
