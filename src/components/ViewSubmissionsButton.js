import React from 'react';

const ViewSubmissionsButton = (props) => {
  return (
    <button type='button' className='btn btn-info btn-block'>
      View Submissions
    </button>
  );
};

export default ViewSubmissionsButton;
