import React from 'react';

const IncompleteItemListButton = (props) => {
  return (
    <button type='button' className='btn btn-warning btn-block'>
      Open Items
    </button>
  );
};

export default IncompleteItemListButton;
